import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.tinyaxolotldart',
  appName: 'ISOEDRE Vision IA',
  webDir: 'dist'
};

export default config;
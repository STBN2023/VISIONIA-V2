import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fr.pragmaia.piavision',
  appName: 'PIA VISION',
  webDir: 'dist'
};

export default config;
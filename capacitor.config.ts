import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.tinyaxolotldart',
  appName: 'tiny-axolotl-dart',
  webDir: 'dist'
};

export default config;
